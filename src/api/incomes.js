import { HEADERS } from "../../api";
export default {
    async updateIncomes(value, year, month) {
        /* DISABLE FOR DEMO
        await fetch(url, {
            ...HEADERS,
            method: "PUT",
            "body": `${JSON.stringify({
                value
            })}`
        })

         */
    }
}